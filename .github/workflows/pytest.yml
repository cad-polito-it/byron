name: Run Python tests

on: [push]

jobs:
  build:
    name: Run tests
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.14"]
        include:
          # Test the upcoming Python 3.15 but don't let it block the PR
          - python-version: "3.15-dev"
            experimental: true

    steps:
      - name: Set system timezone
        uses: szenius/set-timezone@v2.0
        with:
          timezoneLinux: "Europe/Berlin"

      - name: Check out repository
        uses: actions/checkout@v4

      # Combined UV and Python setup into a single optimized step
      - name: Set up uv and Python ${{ matrix.python-version }}
        uses: astral-sh/setup-uv@v5
        with:
          python-version: ${{ matrix.python-version }}
          enable-cache: true

      - name: Running PyTest on Python ${{ matrix.python-version }}
        # Fallback to false if the experimental key is missing
        continue-on-error: ${{ matrix.experimental || false }}
        run: |
          uv sync --all-extras
          uv run pytest